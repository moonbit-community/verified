predicate in_bounds(xs : FixedArray[Int], i : Int) {
  (0 <= i) && (i < xs.length())
}

predicate find_ok(xs : FixedArray[Int], key : Int, result : Int) {
  (result == -1) || ((in_bounds(xs, result)) && (xs[result] == key))
}
