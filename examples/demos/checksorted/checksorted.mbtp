predicate[T : Compare] adj_sorted_upto(xs : FixedArray[T], ok : Int, upto : Int) {
  ∀ k : Int, ((ok == 1) && (0 <= k) && (k + 1 < upto)) →
    xs[k] <= xs[k + 1]
}

predicate[T : Compare] check_sorted_ok(xs : FixedArray[T], result : Int) {
  ∀ k : Int, ((result == 1) && (0 <= k) && (k + 1 < xs.length())) →
    xs[k] <= xs[k + 1]
}
