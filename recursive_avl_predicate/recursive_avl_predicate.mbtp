///|
///|
predicate nonneg(n : Int) {
  0 <= n
}

///|
predicate avl(t : Tree) {
  match t {
    Empty => true
    Node(l, _, r, h) =>
      avl(l) &&
      avl(r) &&
      h == 1 + max2(height(l), height(r)) &&
      -1 <= height(l) - height(r) &&
      height(l) - height(r) <= 1
  }
}
